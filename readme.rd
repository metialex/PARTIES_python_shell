To create the enviroment which is needed to run use the classes from this repository,
run the following commands:

python3 -m venv venv
source venv/bin/activate
pip3 install --upgrade pip
pip3 install -r requirements.txt
